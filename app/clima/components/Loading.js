const Loading = () => {
    return <p style={styles.loading}>Cargando clima...</p>;
  };
  
  const styles = {
    loading: {
      fontSize: '1.5rem',
      color: '#555',
    },
  };
  
  export default Loading;
  